# skcomms realms.yml — realm -> directory_url (config bootstrap for skfed_resolve).
# Generated/merged by scripts/mode-b-setup.sh. Tailnet members resolve this URL
# over WireGuard (the node's 100.x address); there is NO public funnel.
__REALM__: __DIRECTORY_URL__
