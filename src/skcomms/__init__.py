"""skcomms — realm-scoped routing protocol for sovereign AI agents.

This package is the *protocol layer*: three-tier identity
(realm/operator/agent), PGP-signed envelopes, realm-namespaced routing
under ``~/.skcomms/``.

It depends on ``skcomm`` (singular) for the underlying transport plumbing
(Syncthing/IMAP/file/etc).

See ``README.md`` and ``~/clawd/gtd/next/SKCOMMS_REALM_DESIGN.md``.
"""

__version__ = "0.0.1"
